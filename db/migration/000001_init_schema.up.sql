CREATE TABLE `Books` (
                         `id` INT NOT NULL AUTO_INCREMENT,
                         `title` VARCHAR(255),
                         `author` VARCHAR(255),
                         `desc` TEXT,
                         PRIMARY KEY (`id`)
);CREATE TABLE `Books` (
                           `id` INT NOT NULL AUTO_INCREMENT,
                           `title` VARCHAR(255),
                           `author` VARCHAR(255),
                           `desc` TEXT,
                           PRIMARY KEY (`id`)
  );